import { ShowMeDirective } from './show-me.directive';

describe('ShowMeDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowMeDirective();
    expect(directive).toBeTruthy();
  });
});
